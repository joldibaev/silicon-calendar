import { GetMonthDaysCountPipe } from './get-month-days-count.pipe';

describe('GetMonthDaysCountPipe', () => {
  it('create an instance', () => {
    const pipe = new GetMonthDaysCountPipe();
    expect(pipe).toBeTruthy();
  });
});
